{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "sync:legacy": "mkdir -p css js public/css public/js && cp -r resources/legacy-source/css/. css/ && cp -r resources/legacy-source/js/. js/ && cp -r resources/legacy-source/css/. public/css/ && cp -r resources/legacy-source/js/. public/js/",
        "build": "npm run sync:legacy && vite build",
        "dev": "npm run sync:legacy && vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
